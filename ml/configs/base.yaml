project:
  name: summoner-build-lab-ml
  version: 0.1.0
  environment: local

paths:
  artifacts_dir: artifacts
  raw_data_dir: data/raw
  interim_data_dir: data/interim
  processed_data_dir: data/processed
  baseline_model_path: artifacts/models/baseline.joblib
  baseline_metadata_path: artifacts/models/baseline-metadata.json

training:
  random_seed: 42
  test_size: 0.2
  n_samples: 256
  n_features: 8
  n_informative: 5

api:
  host: 0.0.0.0
  port: 8001
  log_level: info

